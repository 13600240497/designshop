{# include 导入root字段
root  加载样式配置项
other 组件配置项 #}

{# 配置组件公共样式配置
1.style-rg.twig统一显示样式
2.
#}
{# form相关组件
background-rg.twig
#}
{# style 相关组件 style-rg.twig #}
{% if root %}
    {% set conponentURL = '@app/files/parts/formTemplate/components/style-rg.twig' %}
{% else %}
    {% set conponentURL = '@app/files/parts/formTemplate/components/'~componentType~'.twig' %}
{% endif %}

{# 组件配置项默认值 #}
{% set defaultParam = {
    box_bg_color: '#F8F8F8'
} %}

{{ include (conponentURL,defaultParam) }}
